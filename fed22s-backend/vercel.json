{
  "version": 2,
  "builds": [
    { "src": "*.ts", "use": "@vercel/node" },
    { "src": "src/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/src/$1"
    }
  ]
}
